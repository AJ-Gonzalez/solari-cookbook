# Seed board tokens, each verified live on 2026-09-02.
# Grow this list over time: a token is the company's board slug, e.g.
#   greenhouse: https://boards-api.greenhouse.io/v1/boards/<token>/jobs
#   lever:      https://api.lever.co/v0/postings/<token>?mode=json

[greenhouse]
tokens = ["gitlab", "stripe", "coinbase", "dropbox", "airbnb", "databricks", "flexport", "samsara", "wizeline", "cloudflare"]

[lever]
tokens = ["rover"]

[ashby]
tokens = ["ashby", "deepgram", "temporal", "replit", "sardine"]

[remoteok]
enabled = true

# [workable]
# tokens = []  # apply.workable.com subdomains — discovery pending
